import React from 'react';
import styled from 'styled-components';

const ChatAuth = () => {
  return <ChatAuthStyle></ChatAuthStyle>;
};

const ChatAuthStyle = styled.div`
  width: 1570px;
  height: 872px;
  position: relative;
`;

export default ChatAuth;
